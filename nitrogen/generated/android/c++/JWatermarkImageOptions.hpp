///
/// JWatermarkImageOptions.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "WatermarkImageOptions.hpp"

#include "JPosition.hpp"
#include "JPositionOptions.hpp"
#include "Position.hpp"
#include "PositionOptions.hpp"
#include <optional>
#include <string>

namespace margelo::nitro::nitroimagemarker {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "WatermarkImageOptions" and the the Kotlin data class "WatermarkImageOptions".
   */
  struct JWatermarkImageOptions final: public jni::JavaClass<JWatermarkImageOptions> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/nitroimagemarker/WatermarkImageOptions;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct WatermarkImageOptions by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    WatermarkImageOptions toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldSrc = clazz->getField<jni::JString>("src");
      jni::local_ref<jni::JString> src = this->getFieldValue(fieldSrc);
      static const auto fieldPosition = clazz->getField<JPositionOptions>("position");
      jni::local_ref<JPositionOptions> position = this->getFieldValue(fieldPosition);
      static const auto fieldScale = clazz->getField<jni::JDouble>("scale");
      jni::local_ref<jni::JDouble> scale = this->getFieldValue(fieldScale);
      static const auto fieldRotate = clazz->getField<jni::JDouble>("rotate");
      jni::local_ref<jni::JDouble> rotate = this->getFieldValue(fieldRotate);
      static const auto fieldAlpha = clazz->getField<jni::JDouble>("alpha");
      jni::local_ref<jni::JDouble> alpha = this->getFieldValue(fieldAlpha);
      return WatermarkImageOptions(
        src->toStdString(),
        position != nullptr ? std::make_optional(position->toCpp()) : std::nullopt,
        scale != nullptr ? std::make_optional(scale->value()) : std::nullopt,
        rotate != nullptr ? std::make_optional(rotate->value()) : std::nullopt,
        alpha != nullptr ? std::make_optional(alpha->value()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JWatermarkImageOptions::javaobject> fromCpp(const WatermarkImageOptions& value) {
      using JSignature = JWatermarkImageOptions(jni::alias_ref<jni::JString>, jni::alias_ref<JPositionOptions>, jni::alias_ref<jni::JDouble>, jni::alias_ref<jni::JDouble>, jni::alias_ref<jni::JDouble>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        jni::make_jstring(value.src),
        value.position.has_value() ? JPositionOptions::fromCpp(value.position.value()) : nullptr,
        value.scale.has_value() ? jni::JDouble::valueOf(value.scale.value()) : nullptr,
        value.rotate.has_value() ? jni::JDouble::valueOf(value.rotate.value()) : nullptr,
        value.alpha.has_value() ? jni::JDouble::valueOf(value.alpha.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::nitroimagemarker
