///
/// JBlurRegion.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "BlurRegion.hpp"

#include <optional>

namespace margelo::nitro::nitroimagemarker {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "BlurRegion" and the the Kotlin data class "BlurRegion".
   */
  struct JBlurRegion final: public jni::JavaClass<JBlurRegion> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/nitroimagemarker/BlurRegion;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct BlurRegion by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    BlurRegion toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldX = clazz->getField<double>("x");
      double x = this->getFieldValue(fieldX);
      static const auto fieldY = clazz->getField<double>("y");
      double y = this->getFieldValue(fieldY);
      static const auto fieldWidth = clazz->getField<double>("width");
      double width = this->getFieldValue(fieldWidth);
      static const auto fieldHeight = clazz->getField<double>("height");
      double height = this->getFieldValue(fieldHeight);
      static const auto fieldBlurRadius = clazz->getField<jni::JDouble>("blurRadius");
      jni::local_ref<jni::JDouble> blurRadius = this->getFieldValue(fieldBlurRadius);
      return BlurRegion(
        x,
        y,
        width,
        height,
        blurRadius != nullptr ? std::make_optional(blurRadius->value()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JBlurRegion::javaobject> fromCpp(const BlurRegion& value) {
      using JSignature = JBlurRegion(double, double, double, double, jni::alias_ref<jni::JDouble>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        value.x,
        value.y,
        value.width,
        value.height,
        value.blurRadius.has_value() ? jni::JDouble::valueOf(value.blurRadius.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::nitroimagemarker
